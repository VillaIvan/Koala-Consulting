---
const tabs = [
  "Automatiza procesos",
  "Expertos Certificados",
  "Experiencia de éxito",
  "Satisfacción Asegurada",
];
let selectedTab = 0;

//const getTabContent = (index) => {
//   switch (index) {
//     case 0:
//       return `
//         <div class="flex flex-col lg:flex-row items-center">
//           <img src="https://via.placeholder.com/150" alt="Imagen de automatización" class="w-full lg:w-1/2 h-auto rounded-lg shadow-lg">
//           <div class="lg:ml-6 mt-4 lg:mt-0 text-center lg:text-left">
//             <h2 class="text-2xl font-bold">CRM, ERP y Low Code Solutions</h2>
//             <p class="mt-2 text-gray-700">Con la implementación de soluciones empresariales puedes disminuir errores, aumentar la velocidad de entrega, impulsar la calidad, minimizar los costes y simplificar tus procesos de negocio a través de la Hiperautomatización.</p>
//           </div>
//         </div>
//       `;
//     case 1:
//       return `
//         <div class="text-center lg:text-left">
//           <h2 class="text-2xl font-bold">Expertos Certificados</h2>
//           <p class="mt-2 text-gray-700">Nuestro equipo de expertos certificados garantiza...</p>
//         </div>
//       `;
//     case 2:
//       return `
//         <div class="text-center lg:text-left">
//           <h2 class="text-2xl font-bold">Experiencia de Éxito</h2>
//           <p class="mt-2 text-gray-700">Contamos con una amplia experiencia en...</p>
//         </div>
//       `;
//     case 3:
//       return `
//         <div class="text-center lg:text-left">
//           <h2 class="text-2xl font-bold">Satisfacción Asegurada</h2>
//           <p class="mt-2 text-gray-700">Nos comprometemos a asegurar la satisfacción de nuestros clientes...</p>
//         </div>
//       `;
//     default:
//       return "";
//   }
// };
---

<style>
  .tab-active {
    @apply text-blue-600 border-blue-600 font-bold;
  }
</style>
<section class="px-10 md:px-0">
  <div class="max-w-4xl mx-auto p-4 bg-white rounded-lg shadow-xl">
    <div
      class="flex flex-wrap justify-center lg:justify-start space-x-0 lg:space-x-4 border-b"
    >
      {
        tabs.map((tab, index) => (
          <button
            type="button"
            class={`py-2 px-4 border-b-2 transition-colors duration-300  ${selectedTab === index ? "tab-active" : "text-gray-600 border-transparent hover:text-blue-600 hover:border-blue-600"}`}
            data-index={index}
          >
            {tab}
          </button>
        ))
      }
    </div>
    <div id="content" class="mt-4 p-4">
      <div class="flex flex-col lg:flex-row items-center">
        <img
          src="https://via.placeholder.com/150"
          alt="Imagen de automatización"
          class="w-full lg:w-1/2 h-auto rounded-lg shadow-lg"
        />
        <div class="lg:ml-6 mt-4 lg:mt-0 text-center lg:text-left">
          <h2 class="text-2xl font-bold">CRM, ERP y Low Code Solutions</h2>
          <p class="mt-2 text-gray-700">
            Con la implementación de soluciones empresariales puedes disminuir
            errores, aumentar la velocidad de entrega, impulsar la calidad,
            minimizar los costes y simplificar tus procesos de negocio a través
            de la Hiperautomatización.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const buttons = document.querySelectorAll("button[data-index]");
    buttons.forEach((button) => {
      button.addEventListener("click", (event) => {
        const target = event.currentTarget as HTMLButtonElement;
        const index = target.getAttribute("data-index");
        handleClick(parseInt(index));
      });
    });
  });

  function handleClick(index) {
    document.querySelectorAll("button").forEach((button, idx) => {
      if (idx === index) {
        button.classList.add("tab-active");
      } else {
        button.classList.remove("tab-active");
      }
    });
    document.querySelector("#content").innerHTML = getTabContent(index);
  }

  function getTabContent(index) {
    switch (index) {
      case 0:
        return `
          <div class="flex flex-col lg:flex-row items-center">
            <img src="https://via.placeholder.com/150" alt="Imagen de automatización" class="w-full lg:w-1/2 h-auto rounded-lg shadow-lg">
            <div class="lg:ml-6 mt-4 lg:mt-0 text-center lg:text-left">
              <h2 class="text-2xl font-bold">CRM, ERP y Low Code Solutions</h2>
              <p class="mt-2 text-gray-700">Con la implementación de soluciones empresariales puedes disminuir errores, aumentar la velocidad de entrega, impulsar la calidad, minimizar los costes y simplificar tus procesos de negocio a través de la Hiperautomatización.</p>
            </div>
          </div>
        `;
      case 1:
        return `
          <div class="text-center lg:text-left">
            <h2 class="text-2xl font-bold">Expertos Certificados</h2>
            <p class="mt-2 text-gray-700">Nuestro equipo de expertos certificados garantiza...</p>
          </div>
        `;
      case 2:
        return `
          <div class="text-center lg:text-left">
            <h2 class="text-2xl font-bold">Experiencia de Éxito</h2>
            <p class="mt-2 text-gray-700">Contamos con una amplia experiencia en...</p>
          </div>
        `;
      case 3:
        return `
          <div class="text-center lg:text-left">
            <h2 class="text-2xl font-bold">Satisfacción Asegurada</h2>
            <p class="mt-2 text-gray-700">Nos comprometemos a asegurar la satisfacción de nuestros clientes...</p>
          </div>
        `;
      default:
        return "";
    }
  }
</script>
